#!/usr/bin/env -S uv run --script
# /// script
# requires-python = ">=3.12"
# dependencies = ["typer>=0.12"]
# ///

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parent))

from sessionlog import run_backend


if __name__ == "__main__":
    run_backend("copilot")
